apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-go
  labels: { app: chat-go, role: sut }
spec:
  replicas: 1
  selector:
    matchLabels: { app: chat-go }
  template:
    metadata:
      labels: { app: chat-go, role: sut }
    spec:
      # sut 노드에만 스케줄 (agent 노드 taint role=sut:NoSchedule 와 세트)
      nodeSelector: { role: sut }
      tolerations:
        - { key: role, operator: Equal, value: sut, effect: NoSchedule }
      containers:
        - name: chat-go
          image: chat-go            # tag 는 kustomization.images 에서 주입
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - { name: SERVER_PORT, value: "8080" }
            - { name: DATABASE_URL, value: postgres://chat:chat1234@postgres:5432/chat_server }
            - { name: PUSH_SERVER_URL, value: http://fake-push-server:8090 }
            - name: JWT_PUBLIC_KEY
              valueFrom:
                secretKeyRef: { name: jwt-public-key, key: public-key }
          # 테스트 대상 리소스 제한 (SR §10: vCPU 1 / RAM 2GB)
          resources:
            requests: { cpu: "100m", memory: 64Mi }
            limits: { cpu: "1", memory: 2Gi }
          startupProbe:
            tcpSocket: { port: 8080 }
            periodSeconds: 2
            failureThreshold: 30
          readinessProbe:
            tcpSocket: { port: 8080 }
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: chat-go
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector: { app: chat-go }
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
