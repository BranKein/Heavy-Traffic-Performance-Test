# 문서 사이트(mkdocs) 전용 의존성 — 애플리케이션과 무관.
# 로컬 미리보기:
#   python3 -m venv .venv-docs && source .venv-docs/bin/activate
#   pip install -r requirements-docs.txt
#   mkdocs serve            # http://127.0.0.1:8000 (저장하면 즉시 반영)
mkdocs-material==9.6.14
