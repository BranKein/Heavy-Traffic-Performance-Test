server:
  port: 8090

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/chat_server
    username: chat
    password: chat1234

# Fake Push Server (SR §7) — 로컬 docker-compose 는 host 9000 → container 8090 로 포워딩
push:
  base-url: http://localhost:9000
  max-concurrency: 256
